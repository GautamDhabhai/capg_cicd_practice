import { test, expect } from '../fixtures';
import { HomePage } from '../../pages/home.page';

test.describe('Footer support and policy links', () => {
  test.fixme('Internal footer links expose meaningful pages', 'The live public footer is not stable enough for repeated request validation; this needs a controlled local fixture or a deterministic page snapshot.');
});
