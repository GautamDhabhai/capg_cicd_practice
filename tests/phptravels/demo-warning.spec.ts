import { test, expect } from '../fixtures';
import { HomePage } from '../../pages/home.page';

test.describe('Demo safeguards', () => {
  test.fixme('Fresh session shows protective demo notice', 'The public demo reopens the overlay or stalls on live network conditions, so this flow is not reliably reproducible outside a controlled fixture environment.');
});
