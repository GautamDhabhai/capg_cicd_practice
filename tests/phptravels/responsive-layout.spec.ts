import { test, expect } from '../fixtures';
import { HomePage } from '../../pages/home.page';

test.describe('Responsive layout', () => {
  test.fixme('Keeps homepage usable at supported viewport widths', 'The public demo intermittently stalls or mutates under viewport changes; run this against a controlled local build instead.');
});
