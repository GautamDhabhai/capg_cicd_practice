import { test, expect } from '../fixtures';

test.describe('Customer and agent signup validation', () => {
  test.fixme('Exposes signup paths without submitting real registrations', 'The public signup menu is blocked by the warning modal in the live demo; this requires a disposable local registration environment.');
});
